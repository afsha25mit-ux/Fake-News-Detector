pipeline {
    agent any

    stages {
        stage('Build Docker Image') {
            steps {
                echo 'Build will be done manually from host'
            }
        }

        stage('Run Container') {
            steps {
                echo 'Run will be done manually from host'
            }
        }
    }
}